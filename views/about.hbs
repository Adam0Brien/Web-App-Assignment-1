{{> menu id="about"}}

<section class="ui center aligned middle aligned segment">
  <h2 class="ui header">
    The movies app team: 
  </h2>

  <div class="ui centered cards">
    {{#each characters}}
      {{> card}}
    {{/each}}
  </div>
</section>
